let sarahAge = 15

if (sarahAge >= 18){ 
    console.log(`sarah can start driving`)
} else{
    ageRequire = 18 - sarahAge
    console.log(`sarah can't start driving. 
wait for ${ageRequire} years `) //use $ to insert value of variable inside string(template literal")
}// use ` notation if you want to use template literal