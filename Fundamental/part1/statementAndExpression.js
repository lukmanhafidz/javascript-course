// Statements and Expressions

//Expressions produce value
//Statements is processing expression to make action

3 + 4 //Expressions
1991 // Expressions
true && false && !false // Expressions

if (23 > 10) { //Statements (Expressions)
  const str = '23 is bigger';
}

const me = 'Jonas'; // Expressions
console.log(`I'm ${2037 - 1991} years old ${me}`); //expecting expression inside bracket
